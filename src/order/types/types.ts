export enum OrderStatus {
  empty = 'empty',
  pending = 'pending',
  confirmed = 'confirmed',
  processing = 'processing',
  shipped = 'shipped',
  delivered = 'delivered',
  canceled = 'canceled',
  returned = 'returned',
  refunded = 'refunded',
  completed = 'completed',
  failed = 'failed',
}
